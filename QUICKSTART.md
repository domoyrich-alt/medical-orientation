# 🚀 Быстрый старт / Quick Start Guide

## Самый простой способ / Easiest Way

### Вариант 1: Открыть напрямую в браузере
1. Откройте файл `index.html` двойным кликом
2. Или перетащите `index.html` в окно браузера

**Важно**: Некоторые функции (Service Worker, PWA) могут не работать при открытии файла напрямую через `file://` протокол.

---

## Рекомендуемый способ / Recommended Way

### Вариант 2: Использовать Python (встроен в большинство систем)

```bash
# Перейдите в папку проекта
cd medical-orientation

# Запустите сервер
python3 -m http.server 8080

# Откройте в браузере
# http://localhost:8080
```

**Для Windows без python3:**
```bash
python -m http.server 8080
```

---

### Вариант 3: Использовать Node.js (если установлен)

```bash
# Установите простой HTTP-сервер глобально (один раз)
npm install -g http-server

# Перейдите в папку проекта
cd medical-orientation

# Запустите сервер
http-server -p 8080

# Или просто
npx http-server -p 8080
```

Откройте в браузере: `http://localhost:8080`

---

### Вариант 4: VS Code Live Server (если используете VS Code)

1. Установите расширение "Live Server" в VS Code
2. Откройте папку проекта в VS Code
3. Правый клик на `index.html` → "Open with Live Server"
4. Автоматически откроется в браузере

---

## Первый запуск / First Launch

После открытия приложения:

1. **Регистрация**:
   - Заполните форму регистрации
   - Введите любой 6-значный код для подтверждения (например: 123456)
   - Это локальная версия, код не проверяется

2. **Начало работы**:
   - Выберите раздел "Тесты" для прохождения тестов
   - Нажмите 🤖 для чата с AI-тьютором
   - Перейдите в "Профиль" для просмотра достижений
   - Используйте переключатель языка (🇷🇺/🇬🇧/🇺🇿) в навигации
   - Переключайте тему (🌙/☀️) для светлого/темного режима

---

## Установка как PWA / Install as PWA

### На компьютере (Chrome/Edge):
1. Откройте приложение через HTTP-сервер
2. В адресной строке нажмите иконку установки (⊕)
3. Или используйте кнопку "Установить приложение" внизу страницы

### На Android:
1. Откройте в Chrome
2. Меню (⋮) → "Установить приложение" или "Добавить на главный экран"

### На iOS:
1. Откройте в Safari
2. Кнопка "Поделиться" (□↑)
3. "На экран «Домой»"

---

## Проверка работы / Testing

### Основные функции:
- ✅ Регистрация и авторизация
- ✅ Прохождение тестов (4 типа тестов)
- ✅ Система достижений
- ✅ AI-тьютор (кнопка 🤖)
- ✅ Ежедневные квесты
- ✅ Таблица лидеров
- ✅ Мультиязычность (RU/EN/UZ)
- ✅ Темная/светлая тема

### PWA функции (работают только через HTTP-сервер):
- ✅ Офлайн режим
- ✅ Установка приложения
- ✅ Push-уведомления (требует настройку VAPID ключа)

---

## Устранение проблем / Troubleshooting

### Проблема: Белый экран
- **Решение**: Откройте консоль браузера (F12) для просмотра ошибок
- Убедитесь, что JavaScript включен в браузере
- Попробуйте другой браузер (Chrome, Firefox)

### Проблема: Service Worker не работает
- **Решение**: Service Worker требует HTTPS или localhost
- Используйте HTTP-сервер (Python/Node.js) вместо file://

### Проблема: PWA не предлагает установку
- **Решение**: 
  - Убедитесь, что используете HTTPS или localhost
  - Проверьте, что manifest.json доступен
  - Очистите кэш браузера и перезагрузите страницу

### Проблема: Функции не работают
- **Решение**:
  - Очистите localStorage: откройте консоль (F12) и выполните `localStorage.clear()`
  - Обновите страницу (Ctrl+F5 / Cmd+Shift+R)

---

## Требования к браузеру / Browser Requirements

### Поддерживаемые браузеры:
- ✅ Chrome 80+ (рекомендуется)
- ✅ Firefox 75+
- ✅ Safari 13.1+
- ✅ Edge 80+

### Необходимые технологии:
- JavaScript ES6+
- LocalStorage
- Service Workers (для PWA)
- CSS Grid & Flexbox

---

## Дополнительная информация / More Info

- 📖 Полная документация: `README.md`
- 🔧 Техническая документация: `IMPLEMENTATION.md`
- 💻 Репозиторий: https://github.com/domoyrich-alt/medical-orientation

---

## Быстрые команды / Quick Commands

```bash
# Клонировать проект
git clone https://github.com/domoyrich-alt/medical-orientation.git
cd medical-orientation

# Запустить (выберите один вариант)
python3 -m http.server 8080        # Python
npx http-server -p 8080            # Node.js
php -S localhost:8080              # PHP

# Открыть в браузере
# http://localhost:8080
```

---

**Готово! Приложение должно работать. Если возникли проблемы, проверьте консоль браузера (F12) для деталей.**
